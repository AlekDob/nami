---
type: diary
project: namios
date: 2026-03-02
---
- [17:00] (Alek) Added text selection (`.textSelection(.enabled)`) and copy icon on user message bubbles in ChatBubble.swift — both iOS and macOS can now select partial text or tap copy to grab the full question
- [18:30] (Alek) Session isolation fix — server: `agent.run()` now accepts per-run `onToolUse` callback (eliminates global mutation race), `tool_use` WS messages tagged with `sessionId`, unified single `SessionStore` instance between CLI and API. Client: introduced `pendingSessionId` to track in-flight response separate from viewed session, WS `.done`/`.toolUse` handlers filter by sessionId, `clearChat()` resets `isThinking` and `pendingSessionId`. Fixes: cron tool_use leaking into chat, old response appearing in new session, loading spinner persisting after session switch.
