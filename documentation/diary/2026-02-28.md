---
type: diary
project: namios
date: 2026-02-28
---

- [10:00] (Alek) fix(macOS): remove deferred markdown rendering that caused LazyVStack thrashing crash. The `@State markdownReady` in ChatBubble was resetting when LazyVStack recycled views, creating infinite height-jump cycles (truncated→full→recycle→truncated→...). Safe to remove because NSTextView was already replaced with native `Text(AttributedString)` on Feb 18 — the deferred rendering was a workaround for NSTextView.ensureLayout() which no longer applies.
- [16:00] (Alek) feat(memory): Second Brain system — knowledge entries con auto-tagging LLM. Nuove tabelle SQLite (knowledge, tags, knowledge_tags) + FTS5. 3 nuovi tool: memorySave, memoryRecall, memoryTag. System prompt aggiornato con istruzioni auto-save. Embeddings abilitati di default se OPENAI_API_KEY presente. Ricerca unificata chunks + knowledge.
- [18:35] (Alek) ui(sessions): resa minimal la lista sessioni — rimosso lastMessage preview dalla SessionRow, ora mostra solo il titolo. Ridotto padding verticale da 10 a 8pt per layout più compatto.
- [19:15] (Alek) feat(design): MyMind Design System — redesign completo dell'app NamiOS ispirato a MyMind. Creati 3 componenti shared: `NamiCardStyle` (ViewModifier per card con ombre/bordi), `NamiEmptyState` (empty state caldo e riutilizzabile), `GradientAccentBar` (barra gradiente 4px). Esteso `MeowTheme` con `Cards` (cornerRadius, shadow params, backgrounds) e `Gradients` (palette per dominio: session, job, command, creation, soul, chat). Riscritte 7 view: SessionListView (card verticali full-width), JobsListView (card con toggle e cron badge), SoulView (visual panels con card selezionabili), AICommandsListView (compact cards con accent bar), OSView (griglia masonry 2 colonne), ModelPickerView (3 preset cards con badge capability). ChatBubble redesignato: messaggi utente con gradiente caldo, messaggi assistente come card con ombre morbide. Send button con arancione accent. Settings: aggiunta card status server con GradientAccentBar verde/rosso. Entrambi i target iOS e macOS compilano clean.
