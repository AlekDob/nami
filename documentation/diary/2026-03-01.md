---
type: diary
project: namios
date: 2026-03-01
---

- [16:10] (Alek) fix(server): model preference ora persiste su disco in `data/model-preference.json`. Prima `setModel()` salvava solo in memoria — al restart del server tornava sempre al default (GLM 4.7). Ora `init()` carica il model salvato, `setModel()` scrive su file. L'utente cambia modello una volta e resta.

- [16:30] (Alek) feat(brain/graph): label visibili su tutti i nodi del grafo, non solo il selezionato. Titoli troncati a 18 char. Nodo selezionato: glow ring + font più grande. Fisica potenziata: repulsione 4x (8000), distanza minima 90pt con hard push, velocity cap ±15. Inizializzazione con spread ampio + jitter random.

- [16:40] (Alek) feat(brain/graph): tap su nodo nel grafo ora naviga al BrainDetailView. Usato `navigationDestination(item:)` con delay 0.4s per aspettare dismiss dello sheet. Il `loadDetail` precedente non navigava — ora chiude lo sheet e apre il dettaglio.

- [16:50] (Alek) feat(brain): broadcastCreation('knowledge') dal tool memory-save. Quando Nami salva nel Brain, il server notifica tutti i client WS. Il client riceve `.creation(type: "knowledge")` → `onKnowledgeSaved` → `brainVM.forceRefresh()`. Refresh real-time senza chiudere/riaprire app.

- [17:00] (Alek) feat(brain): masonry Pinterest adattivo. iOS: 2 colonne fisse. macOS: 2-5 colonne basate su larghezza finestra (260pt/colonna). Algoritmo shortest-first per N colonne. Fix colonne sbilanciate: `minWidth: 0, maxWidth: .infinity` sulle VStack + `frame(minWidth: 0, maxWidth: .infinity)` sull'AsyncImage OG nella BrainCard per impedire espansione oltre il parent.

- [17:05] (Alek) feat(brain): rich text nel BrainDetailView — `Text(entry.content)` sostituito con `MarkdownText(content:)`. Headers, bold, code, liste ora renderizzati.

- [17:10] (Alek) feat(brain): context menu (long press) sulle card Brain. Condividi link (se sourceUrl presente), condividi testo (titolo + summary), elimina. Server: nuovo endpoint `DELETE /api/knowledge/:id` con `deleteKnowledge()` che rimuove entry + tag + vettore embeddings.

- [17:15] (Alek) fix(brain/graph-mac): sheet del grafo su macOS era un mini-popover. Aggiunto `.frame(minWidth: 700, minHeight: 500)` condizionale `#if os(macOS)`.

- [17:20] (Alek) doc: relazioni grafo Brain sono implicite — due nodi collegati se condividono almeno un tag. Weight = numero tag condivisi. O(n²) confronto accettabile con limit 100 nodi. Nessuna relazione esplicita/manuale o semantica (embedding) al momento.
