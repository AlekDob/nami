# === API Keys (set at least one) ===
OPENROUTER_API_KEY=your_openrouter_key_here
# OPENAI_API_KEY=sk-...          # Optional: direct OpenAI (faster)
# ANTHROPIC_API_KEY=sk-ant-...   # Optional: direct Anthropic
# MOONSHOT_API_KEY=sk-...        # Optional: Moonshot/Kimi
# TOGETHER_API_KEY=sk-...        # Optional: Together AI

# === Model Selection ===
# Option 1: Auto-select best available model by tier
# MODEL_PRESET=smart             # fast | smart | pro (default: smart)
#
# Option 2: Force specific model (overrides preset)
# MODEL_NAME=openai/gpt-4o-mini
#
# The system auto-detects your API keys and picks the best model.
# Use /models in CLI to see all available models.
# Use /model <name> to switch at runtime.

# === Legacy provider routing (optional, only if MODEL_NAME is set) ===
# PROVIDER=openrouter             # openrouter | moonshot | together

# === Discord Bot (optional) ===
# DISCORD_TOKEN=
# DISCORD_CLIENT_ID=
# DISCORD_GUILD_ID=

# === IMAP Email (optional) ===
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# IMAP_USER=your_email@gmail.com
# IMAP_PASSWORD=your_app_password

# === X/Twitter API (optional) ===
# X_BEARER_TOKEN=your_bearer_token

# === Data directory ===
DATA_DIR=./data
